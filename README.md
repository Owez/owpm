# owpm

A simple python package manager, like `poetry` or `pipenv`.

## Installing

```bash
pip3 install owpm.py
```

## Using

```bash
owpm --help
```

If that does not work,

```bash
python3 -m owpm --help
```
